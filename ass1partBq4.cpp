#include <iostream>
#include <cmath>
int main() {
float a, b, result;
std::cout << "Enter values for a and b: ";
std::cin >> a >> b;
result = aa + 2ab + bb;
std::cout << "The value of (a+b)^2 is: " << result << std::endl;
return 0;
}
